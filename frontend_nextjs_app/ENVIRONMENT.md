# Frontend environment variables

Create a .env file in frontend_nextjs_app with:

- NEXT_PUBLIC_BACKEND_URL: Base URL for the backend API (e.g., http://localhost:3001)

Notes:
- The app uses read-only pages under /users, /session_tracking, and /app_deployments.
- Styling follows the Ocean Professional theme with Tailwind v4.
